name: Criação da primeira Pipeline
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  primeiro-job:
    name: "Meu primeiro Job"
    runs-on: ubunto-latest
    steps: 
      - name: "Minha primeira ação"
        run : echo "Hello world"
      - name: "Minha segunda ação"
        run : echo "finalizando workflow"